import React from 'react';
import { View, StatusBar } from 'react-native';

const App: React.FC = () => (
  <>
    <StatusBar barStyle="default" />
    <View style={{ flex: 1 }} />
  </>
);

export default App;
